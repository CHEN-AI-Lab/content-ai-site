{
  "name": "content-ai",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "pnpm --filter web dev",
    "build": "turbo build",
    "build:web": "pnpm --filter web build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:unit": "pnpm --filter shared test",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
